@model List<CoriPuno.Entidad.VehiculoEntidad>
@{
    ViewBag.Title = "_DetalleProgramacionPeso";
}
@if (Model != null && Model.Count > 0)
{


    <div class="row">
        @*<div class="col-md-10">
            <label>Labor Origen:</label><label id="LaborName"></label>
            <table id="keywords" cellspacing="0" cellpadding="0">
                <thead>
                    <tr>
                        <th><span>Placa</span></th>
                        <th><span>Contrata</span></th>
                        <th><span>Tara</span></th>
                        <th><span>Hora Pesaje</span></th>
                        <th><span>Peso</span></th>
                        <th><span>Peso Neto</span></th>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td class="placa">@item.Placa</td>
                        <td>@item.Contrata</td>
                        <td>@item.Tara</td>
                        <td>@item.Hora_Pesaje</td>
                        <td>@item.Peso2</td>
                        <td>@item.PesoNeto</td>
                    </tr>
                    }


                </tbody>
            </table>
        </div>*@
        <div class="col-md-12 text-center">
            <div class="form-group">
                <label class="col-md-3 control-label"></label>
                <div class="col-md-6">
                    <input type="submit" id="btn-exportar-detalle" class="btn btn-primary" value="Exportar" />
                </div>
                <label class="col-md-3 control-label"></label>
            </div>
        </div>
    </div>
}
